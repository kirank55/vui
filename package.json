{
  "name": "vui-docs",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npx live-server . --port=3000 --wait=500 --open=/docs/",
    "generate:compat": "node scripts/generate-browser-compat.mjs"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  },
  "dependencies": {
    "caniuse-lite": "^1.0.30001778"
  }
}
